{
  "installCommand": "if [ -d fd ]; then (cd fd && npm install); else npm install; fi",
  "buildCommand": "if [ -d fd ]; then (cd fd && npm run build && rm -rf ../dist && cp -r dist ../dist); else npm run build; fi",
  "outputDirectory": "dist"
}
