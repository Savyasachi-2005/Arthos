{
  "version": 2,
  "buildCommand": "cd fd && npm install && npm run build",
  "outputDirectory": "fd/dist",
  "installCommand": "cd fd && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
